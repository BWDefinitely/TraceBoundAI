# 数据导入导出功能说明

## 功能概述

TraceBound AI 现在支持完整的数据导入导出功能，所有数据存储在客户端浏览器中，完全兼容 Vercel 部署。

## 新增功能

### 1. 数据导出
- 导出所有素材、故事和媒体文件
- 格式：JSON 文件
- 媒体文件转换为 Base64 编码
- 自动命名：`tracebound-backup-YYYY-MM-DD.json`
- 位置：浏览器下载文件夹

### 2. 数据导入
- 支持从导出的 JSON 文件恢复数据
- 自动解析并恢复所有内容：
  - 素材（包括图片）
  - 故事（包括场景图片）
  - 元数据
- 导入会覆盖现有数据（有确认提示）

### 3. 数据管理页面
- 访问路径：首页 → 数据管理
- 功能：
  - 导出所有数据
  - 导入数据文件
  - 查看应用信息

## 使用方法

### 导出备份
1. 访问首页，点击"数据管理"
2. 点击"📥 导出所有数据"
3. 确认导出
4. JSON 文件自动下载到本地

### 导入恢复
1. 访问"数据管理"页面
2. 点击"📤 导入数据文件"
3. 选择之前导出的 JSON 文件
4. 确认导入（会覆盖现有数据）
5. 页面自动刷新加载新数据

## Vercel 部署兼容性

### ✅ 完全兼容
- 所有数据存储在客户端 IndexedDB
- 无服务端文件系统操作
- 无服务端数据库依赖
- 纯静态部署

### 数据存储位置
- **IndexedDB**: 浏览器本地数据库
- **materials**: 素材数据
- **stories**: 故事数据
- **media**: 图片/音频 Blob
- **aiSettings**: AI 配置和 API Key

### 验证结果
✅ 无 `fs` 模块使用
✅ 无 `readFile/writeFile` 操作
✅ 无服务端文件路径
✅ 所有 API 调用在客户端

## 技术实现

### 文件结构
```
src/lib/
  ├── export-import.ts    # 导入导出逻辑
  ├── client-store.ts     # IndexedDB 存储
  └── idb.ts             # IndexedDB 封装

src/app/
  └── settings/
      └── page.tsx        # 数据管理页面
```

### 导出格式
```json
{
  "version": "1.0.0",
  "exportDate": "2026-07-31T12:00:00.000Z",
  "materials": [...],
  "stories": [...],
  "media": {
    "material-id": "base64-encoded-data",
    ...
  }
}
```

## 注意事项

### 数据安全
⚠️ 导出的 JSON 文件包含：
- 所有素材和故事内容
- AI API Key（存储在 aiSettings 中）
- 媒体文件（Base64 编码）

**建议：**
- 妥善保管导出文件
- 不要上传到公开位置
- 定期更新备份

### 存储限制
- Chrome: ~60% 可用磁盘空间
- Firefox: ~50% 可用磁盘空间  
- Safari: ~1GB

### 跨设备使用
数据不会自动同步，需要：
1. 设备 A 导出数据
2. 传输 JSON 文件到设备 B
3. 设备 B 导入数据

## 常见问题

### Q: 数据会丢失吗？
A: IndexedDB 数据持久化，但清除浏览器数据会删除。建议定期导出备份。

### Q: 可以跨浏览器使用吗？
A: IndexedDB 独立于每个浏览器。需要导出/导入来迁移数据。

### Q: 导出文件很大怎么办？
A: 媒体文件会转换为 Base64，体积增大约 33%。可以：
- 压缩图片后再导入
- 清理不需要的素材
- 分批导出

### Q: 导入失败怎么办？
A: 检查：
- JSON 文件格式是否正确
- 浏览器存储配额是否充足
- 是否在隐私模式（IndexedDB 受限）

### Q: Vercel 部署后数据在哪？
A: 所有数据在用户浏览器的 IndexedDB 中，不在服务器。每个用户的数据独立。

## 备份建议

### 定期备份
- 每周导出一次
- 重要内容立即备份
- 多设备保留副本

### 存储位置
- 本地硬盘
- 云盘（Google Drive、OneDrive）
- NAS 或外置存储

### 版本管理
- 保留多个时间点的备份
- 命名包含日期
- 重要版本单独标记

## 更新日志

### 2026-07-31
- ✅ 添加数据导出功能
- ✅ 添加数据导入功能
- ✅ 创建数据管理页面
- ✅ 验证 Vercel 部署兼容性
- ✅ 添加部署文档

## 相关文档

- [部署指南](./DEPLOYMENT.md)
- [Vercel兼容性检查](./VERCEL-COMPATIBILITY.md)
